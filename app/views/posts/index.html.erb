<!--<ul>
    <% @posts.each do |post| %>
        <li><%= image_tag(post.image.url) if post.image %>
            <%= post.title %></li>
    <% end %>
</ul>-->

<ul>
    <% @posts.each do |post| %>
        <li><%= link_to image_tag(post.image.url), post %>
            <%= post.title %></li>
    <% end %>
</ul>